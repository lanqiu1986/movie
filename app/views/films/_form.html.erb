<%= form_for @film do |f| %>
<% if @film.errors.any? %>
<ul>
  <% @film.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
</ul>
<% end %>
Name
<%= f.text_field :name %>
<br>
叙述
<br>
<%= f.text_area :description %>
<br>

<%= f.submit "Submit", :disable_with => 'Submiting...' %>
<% end %>
